NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=

# Editor-role API token, used only by local scripts (scripts/ingest.ts,
# scripts/seed.ts). Create one at https://www.sanity.io/manage — never commit
# the real value.
SANITY_WRITE_TOKEN=
